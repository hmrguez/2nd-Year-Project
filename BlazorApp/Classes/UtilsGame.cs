using Domino;

public static class UtilsGame
{
    public static GameObject[] GetGamesTournament(InstancesSettings instances)
    {
        GameObject[] games = new GameObject[3];

        for (int i = 0; i < games.Length; i++)
        {
            //games[i]
        }
        return games;
    }
}